import { environment } from "src/environments/environment";

export const LOGIN = `${environment.webApi}/user/Authentication/Login`;
export const LOGOUT = `${environment.webApi}/user/Authentication/SetLogoutTime`;